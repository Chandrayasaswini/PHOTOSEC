pipeline {
    agent any
    stages {
        stage('Fetch Code') {
            steps {
                git credentialsId: 'git', branch: 'main', url: 'https://github.com/Chandrayasaswini/PHOTOSEC.git'
            }
        }
        stage('Code Analysis') {
            steps {
                script {
                    withSonarQubeEnv('SonarQube') {
                        sh "${scannerHome}/bin/sonar-scanner " +
                           "-Dsonar.projectKey=tech " +
                           "-Dsonar.projectName=MNP_Tech " +
                           "-Dsonar.projectVersion=1.0 " +
                           "-Dsonar.sources=."
                    }
                }
            }
        }
    }
}
