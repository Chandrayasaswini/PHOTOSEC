pipeline {
    agent any
    
    stages {
        stage('Fetch Code') {
            steps {
                git branch: 'main', url: 'https://github.com/Chandrayasaswini/PHOTOSEC.git'
            }
        }
        stage('Code Analysis') {
            steps {
                script {
                    withSonarQubeEnv('sonar') {
                        sh "${scannerHome}/bin/sonar-scanner " +
                           "-Dsonar.projectKey=MNP_Tech " +
                           "-Dsonar.projectName=MNP_Tech " +
                           "-Dsonar.projectVersion=1.0 " +
                           "-Dsonar.sources=."
                    }
                }
            }
        }
    }
}
