pipeline{
    agent any
    stages{
        stage("checkout"){
            steps{
                 echo "checkout scm"
            }
        }
        stage("Build"){
            steps{
                echo "The build is done"
            }
        }
        stage("Deploy"){
            steps{
                echo "The deploye is done"
            }
        }
    }
}
